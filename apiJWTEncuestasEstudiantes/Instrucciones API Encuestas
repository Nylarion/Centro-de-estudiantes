Paso 1: Crear un Usuario (Registro)

Método: POST

URL: http://localhost:3000/api/auth/register

Body (JSON):

JSON

{
  "email": "bpalma2025@alu.uct.cl",
  "password": "password123"
}

Paso 2: Convertirlo en Administrador (Manual)

Abre MongoDB Compass.

Dale al botón Refresh 🔄 en tu colección users.

Busca a bpalma2025@alu.uct.cl.

Haz clic en el lápiz (Edit) del documento.

Cambia el campo "role": "student" por:

"role": "admin"

Paso 3: Iniciar Sesión (Login de Admin)

Método: POST

URL: http://localhost:3000/api/auth/login

Body (JSON):

JSON

{
  "email": "bpalma2025@alu.uct.cl",
  "password": "password123"
}

Copia el código largo que aparece en "token": "eyJhbGci...".

Paso 4: Crear una Encuesta

Método: POST

URL: http://localhost:3000/api/surveys

Pestaña Authorization (Auth):

Tipo: Bearer Token.

Token: Pega aquí el token que copiaste en el paso 3.

Body (JSON):

JSON

{
  "title": "¿Qué banda quieren para el aniversario UCT?",
  "description": "Voten por su favorita",
  "options": ["Los Bunkers", "Los Tres", "Los Prisioneros"]
}

Paso 5: Listar Encuestas (Verificar Redis)
Método: GET

URL: http://localhost:3000/api/surveys

Auth: Asegúrate de tener el Token puesto

Send (1ra vez): Tarda un poco (lee de Mongo).

Send (2da vez): Es instantáneo (lee de Redis).
